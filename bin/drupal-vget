#!/bin/bash
# 23.02.2015
# v0.2
# отказ от drush ради скорости

#v0.1
#val=$(drush vget "$1" --exact 2>&1 | grep "^$1: " | cut -d':' -f2 | tr -d ' ' | sed "s/'//g" | grep -v ERROR)
#if [ -z "$val" ]; then val=0; fi
#echo $val

drupal-sql "SELECT value FROM {variable} WHERE name='$1'" | drupal-decode
